{
  "name": "glas-croatian-transcriber",
  "version": "1.0.0",
  "private": true,
  "description": "Glas — a Croatian voice-note transcriber on Cloudflare Pages + ElevenLabs Scribe.",
  "type": "module",
  "scripts": {
    "build": "echo 'No build step — static assets in /public are served as-is; Pages compiles /functions automatically.'",
    "dev": "wrangler pages dev public",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0"
  }
}
